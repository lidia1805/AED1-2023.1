#include "lista_sequencial.h"

int main(){
    Lista *li;
    li = cria_lista();
    int x = tamanho_lista(li)
    int insere_lista_final(li, dados_aluno);
    libera_lista(li);
	return 0;
}

 
